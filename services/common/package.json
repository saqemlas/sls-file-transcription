{
  "name": "@audio-analysis/common",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rimraf node_modules .serverless",
    "checktsc": "tsc --noEmit",
    "test": "jest",
    "coverage": "jest --collect-coverage",
    "deploy": "yarn run serverless deploy",
    "remove": "yarn run serverless remove"
  },
  "dependencies": {
    "aws-lambda": "1.0.6",
    "aws-xray-sdk-core": "3.3.6",
    "winston": "3.3.3"
  },
  "devDependencies": {
    "@audio-analysis/config": "1.0.0",
    "@types/aws-lambda": "8.10.83"
  }
}
